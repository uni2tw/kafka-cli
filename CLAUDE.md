# CLAUDE.md — kafka-cli（個人專案）

## 專案性質
這個專案原本是 PXEC 內部工具（fork 自開源 [olesmartyniuk/kafka-cli](https://github.com/olesmartyniuk/kafka-cli)），
但現在是**個人專案**，不再受 `~/.claude/CLAUDE.md` 裡公司規範的約束。以下規則**覆寫**全域設定，僅適用於本專案目錄。

## Git 規範（覆寫全域「Git 規範」）
- 可以直接在 `main` 分支上 commit，不需要先建立 feature 分支
- 不需要遵循 `feature/unicorn/ECB2E-{ticket}` 這種內部命名規則
- `origin` 指向個人 GitHub：`https://github.com/uni2tw/kafka-cli.git`（不要改回內部 PXEC repo）

## 其他
全域 CLAUDE.md 裡跟「PXEC 系統開發」相關的內容（資料庫規則、DAO 規範、頁面權限、訂單/貨態等）與本專案無關，不適用。
